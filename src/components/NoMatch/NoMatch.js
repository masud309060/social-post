import React from 'react';

const NoMatch = () => {
    return (
        <div style={{textAlign: "center", alignItems: "center"}}>
            <h2 style={{color: "#f10000"}}>404 Error!!!</h2>
            <h3 style={{color: "#333"}}>No Match data found</h3>
        </div>
    );
};

export default NoMatch;